Run
```
pip install -r requirements.txt
```

```
1.Login via terminal using az login
2.After try az account show
3.Copy id and set it as SUBSCRIPTION_ID in your .env file
4.run python resourcecrude.py
```